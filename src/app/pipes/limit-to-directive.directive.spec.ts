import { LimitToDirectiveDirective } from './limit-to-directive.directive';

describe('LimitToDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitToDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
